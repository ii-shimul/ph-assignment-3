module.exports = {
  theme: {
    extend: {
      screens: {
        "2xl": "1536px", // Default 2xl breakpoint
        "3xl": "1920px", // Custom breakpoint
      },
      maxWidth: {
        "8xl": "1920px", // Custom max-width for container
        full: "100%", // Ensure full width if needed
      },
    },
  },
  plugins: [],
};
